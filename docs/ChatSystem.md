# Система чата через Socket.IO

## Описание

Встроенная система чата для общения пользователей с администраторами через Socket.IO. Работает в реальном времени без внешних зависимостей.

## Архитектура

### Backend

- **Модели**: `Chat` и `Message` в MongoDB
- **Socket.IO сервер**: Real-time общение
- **REST API**: Для загрузки истории и управления чатами
- **Аутентификация**: JWT токены для Socket.IO и REST API

### Frontend

- **SupportChat**: Компонент для пользователей (плавающая кнопка)
- **AdminChat**: Страница для админов с списком всех чатов
- **Socket.IO клиент**: Подключение к серверу

## Установка

### Backend

```bash
cd server
npm install
```

Добавьте в `server/.env`:
```env
JWT_SECRET=your_secret_key
MONGODB_URI=your_mongodb_uri
```

### Frontend

```bash
cd client
npm install
```

## Использование

### Для пользователей

1. Войдите в систему
2. На главной странице появится плавающая кнопка чата (💬) в правом нижнем углу
3. Нажмите на кнопку, чтобы открыть чат
4. Напишите сообщение администратору
5. Получайте ответы в реальном времени

### Для администраторов

1. Войдите как администратор
2. Перейдите в админ-панель (`/admin`)
3. Нажмите кнопку "Chats" в верхней части
4. Или перейдите напрямую на `/admin/chat`
5. Выберите чат из списка
6. Отвечайте пользователям в реальном времени

## API Endpoints

### REST API

- `GET /api/chat/user/chat` - Получить чат текущего пользователя
- `GET /api/chat/chats` - Получить все чаты (для админов)
- `GET /api/chat/chats/:chatId` - Получить историю чата
- `POST /api/chat/send` - Отправить сообщение
- `POST /api/chat/chats/:chatId/read` - Пометить как прочитанное
- `GET /api/chat/unread-count` - Получить количество непрочитанных

### Socket.IO Events

#### Клиент → Сервер

- `get-chat` - Запросить данные чата
- `send-message` - Отправить сообщение
- `mark-read` - Пометить сообщения как прочитанные
- `get-unread-count` - Получить количество непрочитанных

#### Сервер → Клиент

- `chat-data` - Данные чата (для пользователя)
- `chats-list` - Список чатов (для админов)
- `new-message` - Новое сообщение
- `messages-read` - Сообщения прочитаны
- `unread-count` - Количество непрочитанных
- `error` - Ошибка

## Структура данных

### Chat Model

```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User),
  username: String,
  status: 'active' | 'closed' | 'pending',
  lastMessageAt: Date,
  createdAt: Date
}
```

### Message Model

```javascript
{
  _id: ObjectId,
  chatId: ObjectId (ref: Chat),
  from: 'user' | 'admin',
  text: String,
  timestamp: Date,
  read: Boolean
}
```

## Особенности

1. **Real-time общение**: Сообщения доставляются мгновенно через Socket.IO
2. **Автоматическое создание чата**: Чат создается при первом сообщении пользователя
3. **Уведомления**: Счетчики непрочитанных сообщений
4. **История**: Все сообщения сохраняются в MongoDB
5. **Автоматическая прокрутка**: Новые сообщения автоматически прокручиваются вниз

## Troubleshooting

### Socket.IO не подключается

- Проверьте, что сервер запущен
- Проверьте токен в localStorage
- Проверьте CORS настройки в `server/index.js`

### Сообщения не отправляются

- Проверьте консоль браузера на ошибки
- Убедитесь, что пользователь авторизован
- Проверьте подключение к MongoDB

### Админ не видит чаты

- Убедитесь, что пользователь имеет права администратора
- Проверьте, что чаты созданы (пользователь отправил хотя бы одно сообщение)
